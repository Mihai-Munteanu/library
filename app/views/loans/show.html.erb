<%
  # Prepare basic information fields
  basic_fields = [
    {
      label: "Member",
      value: link_to(@loan.member.name, member_path(@loan.member), class: "text-blue-600 hover:text-blue-800 hover:underline")
    },
    {
      label: "Book",
      value: link_to(@loan.book.title, book_path(@loan.book), class: "text-blue-600 hover:text-blue-800 hover:underline")
    },
    { label: "Start Date", value: @loan.start_date || "N/A" },
    { label: "Due Date", value: @loan.due_date || "N/A" },
    { label: "Return Date", value: @loan.return_date || "N/A" },
    { label: "Status", value: @loan.status || "N/A" },
    { label: "Notes", value: @loan.notes || "N/A", col_span: "sm:col-span-2 lg:col-span-3", value_class: "text-base text-gray-900 leading-relaxed" },
    { label: "Metadata", value: @loan.metadata || "N/A", col_span: "sm:col-span-2 lg:col-span-3", value_class: "text-base text-gray-900" },
    { label: "Paused Start Time", value: @loan.paused_start_time || "N/A" },
    { label: "Paused End Time", value: @loan.paused_end_time || "N/A" }
  ]
%>
<%= render "shared/show_page",
    resource: @loan,
    title: "#{@loan.member.name} - #{@loan.book.title}",
    index_path: loans_path,
    edit_path: edit_loan_path(@loan),
    delete_confirmation_path: delete_confirmation_loan_path(@loan, from_page: "show"),
    details_sections: [
      {
        title: "Basic Information",
        fields: basic_fields
      }
    ] %>
